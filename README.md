# **DATA - ETL**

- Demo ETL(Extract - Transform - Load) workflows across multiple data formats
- process and clean structures and semi-structured data ---> includes XML, JSON and PDF files

## **Overview**
- Parsing and transforming XML data into HTML using Python and XSLT.
- Cleaning and Normalizing JSON data into CSV or validated JSON.
- Extracting text and validating content from PDF documents.
- Implementing logging and validation for robust ETL pipelines.
- Automating workflows with python scripts and optional CLI commands.

## **Features**
- Modular ETL scripts for different data formats
- XML → HTML transformation using XSLT
- PDF text extraction and validation
- JSON cleaning and normalization
- Command-line interface to run ETL pipelines
- Logging, validation, and error handling
- Unit tests to ensure ETL reliability

XML  ---> structured technical data, parsed and transformed into HTML
JSON ---> nested JSON files, normalized and cleaned
PDF  ---> extract content and validate document integrity 

